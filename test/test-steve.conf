#!/bin/sh

# Path to the queue
QUEUE=./steve-queue

# Path to the working directory
WORKING_DIR=./steve-working

# Path to the notifier script
NOTIFIER=`pwd -P`/prowlstub.sh

# Path to the PID file
STEVE_PID_FILE=./steve.pid
